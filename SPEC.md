# CODE CRACKER — 現行仕様

## 概要

60秒以内に、重複しない4桁の数字コードを当てる短時間ミニゲーム。
スマートフォンでは画面内テンキー、PCでは物理キーボードでも操作できる。

## ゲームルール

- 正解コードは **0〜9から4桁**。
- 同じ数字はコード内で重複しない。
- プレイヤーの入力も同じ数字を重複して使えない。
- 制限時間は **60秒**。
- 4桁入力後に `ENTER` で判定する。
- 履歴一覧は表示しない。
- 不正解の場合、判定演出のあと入力欄をリセットして次の試行へ進む。

## 判定

各桁の下のバーを、左から順番に1つずつ表示する。

- **緑**: 数字も位置も正しい。
- **黄**: 数字はコード内にあるが、位置が違う。
- **赤**: 数字がコード内にない。

判定は4つ同時表示ではなく、短い効果音とともに順番に表示する。

## 画面構成

### タイトル

- `CODE CRACKER`
- `START`
- 右上に `BLACK / WHITE` テーマ切替

英字モードは使用しない。数字4桁専用。

### ゲーム画面

- `EXIT`
- 残り時間
- タイマーゲージ
- 4つの数字表示枠
- 各枠の下に判定バー
- 画面下部にスマホ用テンキー

テンキー配置:

```text
1 2 3 4 5
6 7 8 9 0
DELETE   ENTER
```

### リザルト

成功時:

- `CODE CRACKED`
- 正解コード
- 経過時間
- 試行回数
- `RETRY`
- `TITLE`

失敗時:

- `TIME UP`
- 正解コード
- 経過時間
- 試行回数
- `RETRY`
- `TITLE`

## 操作

### スマートフォン

- 数字ボタン: 左から数字を入力
- `DELETE`: 最後に入力した数字を削除
- `ENTER`: 4桁入力済みのときだけ判定
- `EXIT`: ゲームを終了してタイトルへ戻る

### PC

- `0〜9`: 数字入力
- `Backspace` / `Delete`: 最後の数字を削除
- `Enter`: 判定

## テーマ

- BLACK
- WHITE

タイトル画面右上のボタンで切り替える。
選択中のテーマはゲーム画面・リザルト画面にも引き継ぐ。

## 実装方針

- 単一の `index.html` で動作する。
- 外部ライブラリ不要。
- サーバー処理不要。
- スマホ縦画面を優先しつつPCでも操作可能。
